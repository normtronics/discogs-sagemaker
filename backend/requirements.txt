# PyTorch: install from their index (has wheels for Intel Mac + Apple Silicon)
# If "No matching distribution" error, run first: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0

# Web framework
fastapi>=0.115.0,<0.135.0
uvicorn>=0.32.0,<0.35.0
python-multipart>=0.0.12

# Image processing (Pillow >=11.3.0 fixes CVE-2025-48379)
pillow>=11.3.0,<12.0.0

# HTTP & auth
requests>=2.31.0,<3.0.0
certifi>=2024.0.0
requests-oauthlib>=2.0.0

# Discogs
python3-discogs-client>=2.8.0

# Config & utils
python-dotenv>=1.0.1
pydantic>=2.10.0,<3.0.0
aiofiles>=24.1.0

# ML (numpy<2 for PyTorch compatibility; 2.x can cause "compiled with NumPy 1.x" crash)
numpy>=1.26.0,<2.0.0
scikit-learn>=1.5.0,<2.0.0

# Hugging Face (for push_to_huggingface.py)
huggingface_hub>=0.20.0

# SageMaker dependencies
boto3>=1.35.0
sagemaker>=2.200.0,<3.0.0  # Version 3.x removed sagemaker.pytorch
aiohttp>=3.8.0
